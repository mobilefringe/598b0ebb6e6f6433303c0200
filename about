<div class="paused">Paused</div>
<div class="slick-slider" id="banner_container">
    <script id="banner_template" type="x-tmpl-mustache/text">
        
            <a href="{{url}}" {{css}} onclick="{{noLink}}">
        	    <div class="bottom image_medium" style="{{banner_image}}"></div>
            </a>
        
    </script>
</div>    
<div id="maincontent" class="container">
    <div class="sixteen clearfix content">
	    <div class="four">
		    <div class="left-sidebar top-padding">
                <a href="/about" class="active">Overview</a>
                <a href="/about/team" class="">Team</a>
                <a href="/about/accomplishments" class="">Accomplishments</a>
                <a href="/about/community" class="">Community</a>
                <a href="/about/careers" class="">Careers</a>
                <a href="/about/news" class="">News</a>
            </div>	
        </div>	
	    <div class="twelve pagecontent">	
			<h1>About</h1>
		    <div id="page_content"></div>
	    </div>		
    </div>
</div>

<script>
    $(document).ready(function(e){
        init(e);
        function renderAll(){
            var repo = getRepoDetailsByName("About Banner");
            if (repo != undefined) {
                var banner_images = repo.images;
                $.each(banner_images, function(key, val) {
                    console.log("Hello!")
                //     var container = $(".slick-slider");
                //     // var gallery_slug = $(val).attr('source');
                //     // gallery = getFashionBySlug(gallery_slug).fashion_images;
                //     renderBanner(container, "#banner_template", banner_images);
                });
                
                renderBanner("#banner_container", "#banner_template", banner_images);
            }
            
            var prefix = get_prefix();
            var pages_json = prefix + "/pages/20vic-about.json"
            $.getJSON(pages_json).done(function(data) {
                $('#page_content').html(data.body);
            }).fail(function(jqXHR) {
                if (jqXHR.status == 404) {
                    $("#404_msg").fadeIn("fast");
                }
            }); 
            
            show_content();
            
            $('.slick-slider').slick({
                arrows: false,
                dots: true,
                autoplay: true,
                autoplaySpeed: 5000  
            });
        }
        loadMallDataCached(renderAll); 
    });
</script>